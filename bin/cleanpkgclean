#!/usr/bin/bash

pup="$(comm -23 <(sort <(cat ~/.config/cleanup/essentials ~/.config/cleanup/ignore)) <(pacman -Qqe | sort))"
if [ -z "$pup" ]; then
	echo "No unwanted entries!"
else
	echo "Potentially unwanted entries:"
	printf "%s\n" "$pup"
fi
