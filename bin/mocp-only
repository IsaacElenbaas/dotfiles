#!/bin/bash

{ mocp --clear && mocp --exit; } &>/dev/null
while [ -f ~/.moc/pid ]; do sleep 0.1; done
mocp --server &>/dev/null
mocp --on shuffle --append "${@:-.}"
herbstclient emit_hook song 2>/dev/null || true
