autodetach off
# some have issues for no reason (eg. ^[O) so find another thing to put it on
# ^[^A will not work for any letters, so sadly can't double possible keys
# some just don't work on certain keys, eg. ^[Q can be on screen but not resize -h -12
escape "^Yy"
bindkey "^[G"
# bindkey "^[R"
bindkey "^[D"
bindkey "^[Q"
bindkey "^[^V"
bindkey "^[H"
bindkey "^[T"
bindkey "^[C"
bindkey "^[N"
bindkey "^[L"
bindkey "^[\177"
bindkey "^['"
bindkey "^[,"
bindkey "^[M"
bindkey "^[S"
bindkey "^[V"
bindkey "^[E"
bindkey "^[."
bindkey "^[U"
bindkey "^[B"
